# dataset-curation-pipeline
Machine learning data pipeline for large-scale image dataset curation, using DINOv2 embeddings, vector similarity search,  and k-center sampling to transfer and filter millions of raw images into a high quality training dataset.
